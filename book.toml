[book]
title = "sāf.ai Resiliate 2.0 Administrator's Guide"
authors = ["Ahmed Masud", "Joey Phan"]
description = "Resiliate Documentation"
src = "src"
language = "en"
multilingual = false

[build]
build-dir = "book"
create-missing = true
use-default-preprocessors = true
extra-watch-dirs = []

[preprocessor.index]

[preprocessor.links]
turn-off = false

[preprocessor.hide]

[preprocessor.plantuml]
plantuml-cmd = "/home/masud/.local/bin/plantuml"
# plantuml-cmd="http://localhost:8080/plantuml"
use-data-uris = true

[preprocessor.mermaid]
command = "mdbook-mermaid"

[preprocessor.toc]
command = "mdbook-toc"
renderer = ["html"]

[preprocessor.theme]
# un comment this to rebuild the theme
turn-off = true
pagetoc = false
pagetoc-width = "13%"
sidebar-width = "300px"
content-max-width = "1100px"

[preprocessor.admonish]
command = "mdbook-admonish"
assets_version = "3.1.0"    # do not edit: managed by `mdbook-admonish install`
custom-types = ["optional"]

[output]

[output.html]
additional-js = [
	"src/mermaid.min.js",
	"src/mermaid-init.js",
	"theme/pagetoc.js",
	"src/external-links.js",
]
additional-css = ["theme/pagetoc.css", "./mdbook-admonish.css"]

# [output.html.search]
# enable = true            # enables the search feature
# limit-results = 30       # maximum number of search results
# teaser-word-count = 30   # number of words used for a search result teaser
# use-boolean-and = true   # multiple search terms must all match
# boost-title = 2          # ranking boost factor for matches in headers
# boost-hierarchy = 1      # ranking boost factor for matches in page names
# boost-paragraph = 1      # ranking boost factor for matches in text
# expand = true            # partial words will match longer terms
